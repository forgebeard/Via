<p><strong>{{ emoji | default('📝') }}</strong> {{ title | default('Изменение') }} <a href="{{ issue_url }}">#{{ issue_id }}</a></p>
<p>{{ subject }}</p>
<p>Тип: {{ event_type }}{% if extra_text %} — {{ extra_text }}{% endif %}</p>
