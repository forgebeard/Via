"""
Пакет верхнего уровня репозитория: код приложения в подпакетах `bot`, `admin`, `database` и др.

Импорты в модулях: `from bot...`, `from config...` при PYTHONPATH, указывающем на каталог `src/`
(локально, в CI и в образе Docker — см. Dockerfile и `.cursorrules`).

Точка входа бота: `python -m src.bot.main` (пакет `src` относительно рабочего каталога `/app` в контейнере).
"""
